{
  "session_id": "fd7af397-89c7-4507-bbc3-10001d3a3e94",
  "transcript_path": "/home/can/.claude/projects/-home-can-Desktop-Dev-AI-Claude-Code-Masterclass/fd7af397-89c7-4507-bbc3-10001d3a3e94.jsonl",
  "cwd": "/home/can/Desktop/Dev/AI/Claude-Code-Masterclass",
  "prompt_id": "bb905e02-52ea-4e78-97c7-a051634386a9",
  "permission_mode": "acceptEdits",
  "effort": {
    "level": "high"
  },
  "hook_event_name": "PostToolUse",
  "tool_name": "Edit",
  "tool_input": {
    "file_path": "/home/can/Desktop/Dev/AI/Claude-Code-Masterclass/hooks/useHeists.ts",
    "old_string": "    const unsubscribe = onSnapshot(q, (snapshot) => {\n      let docs = snapshot.docs.map((doc) => doc.data());\n      if (mode === \"expired\") {\n        docs = docs.filter((h) => h.finalStatus !== null);\n      }\n      setHeists(docs);",
    "new_string": "    const unsubscribe = onSnapshot(q, (snapshot) => {\n      let docs: Heist[] = snapshot.docs.map((doc) => doc.data() as Heist);\n      if (mode === \"expired\") {\n        docs = docs.filter((h) => h.finalStatus !== null);\n      }\n      setHeists(docs);",
    "replace_all": false
  },
  "tool_response": {
    "filePath": "/home/can/Desktop/Dev/AI/Claude-Code-Masterclass/hooks/useHeists.ts",
    "oldString": "    const unsubscribe = onSnapshot(q, (snapshot) => {\n      let docs = snapshot.docs.map((doc) => doc.data());\n      if (mode === \"expired\") {\n        docs = docs.filter((h) => h.finalStatus !== null);\n      }\n      setHeists(docs);",
    "newString": "    const unsubscribe = onSnapshot(q, (snapshot) => {\n      let docs: Heist[] = snapshot.docs.map((doc) => doc.data() as Heist);\n      if (mode === \"expired\") {\n        docs = docs.filter((h) => h.finalStatus !== null);\n      }\n      setHeists(docs);",
    "originalFile": "\"use client\";\n\nimport { useState, useEffect } from \"react\";\nimport {\n  collection,\n  query,\n  where,\n  onSnapshot,\n  Timestamp,\n} from \"firebase/firestore\";\nimport db from \"@/lib/db\";\nimport { COLLECTIONS, heistConverter } from \"@/types/firestore\";\nimport type { Heist } from \"@/types/firestore\";\nimport { useUser } from \"@/hooks/useUser\";\n\nexport type HeistMode = \"active\" | \"assigned-by-me\" | \"expired\";\n\nexport function useHeists(mode: HeistMode): {\n  heists: Heist[];\n  loading: boolean;\n} {\n  const { uid } = useUser();\n  const [heists, setHeists] = useState<Heist[]>([]);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    if (mode !== \"expired\" && !uid) return;\n\n    const ref = collection(db, COLLECTIONS.HEISTS).withConverter(\n      heistConverter,\n    );\n    const now = Timestamp.now();\n\n    const q =\n      mode === \"active\"\n        ? query(\n            ref,\n            where(\"assignedTo\", \"==\", uid),\n            where(\"deadline\", \">\", now),\n          )\n        : mode === \"assigned-by-me\"\n          ? query(\n              ref,\n              where(\"createdBy\", \"==\", uid),\n              where(\"deadline\", \">\", now),\n            )\n          : query(ref, where(\"deadline\", \"<\", now));\n\n    const unsubscribe = onSnapshot(q, (snapshot) => {\n      let docs = snapshot.docs.map((doc) => doc.data());\n      if (mode === \"expired\") {\n        docs = docs.filter((h) => h.finalStatus !== null);\n      }\n      setHeists(docs);\n      setLoading(false);\n    });\n\n    return unsubscribe;\n  }, [mode, uid]);\n\n  return { heists, loading };\n}\n",
    "structuredPatch": [
      {
        "oldStart": 47,
        "oldLines": 7,
        "newStart": 47,
        "newLines": 7,
        "lines": [
          "           : query(ref, where(\"deadline\", \"<\", now));",
          " ",
          "     const unsubscribe = onSnapshot(q, (snapshot) => {",
          "-      let docs = snapshot.docs.map((doc) => doc.data());",
          "+      let docs: Heist[] = snapshot.docs.map((doc) => doc.data() as Heist);",
          "       if (mode === \"expired\") {",
          "         docs = docs.filter((h) => h.finalStatus !== null);",
          "       }"
        ]
      }
    ],
    "userModified": false,
    "replaceAll": false
  },
  "tool_use_id": "toolu_01J2Wku1cD7xpFYjNKruJ6Pt",
  "duration_ms": 60
}
